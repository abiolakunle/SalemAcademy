import "./AppPageTitle.css"
const AppPageTitle = ({ title }: any) => {
    return (
        <div className='app-page-title'>
            <h2>{title}</h2>
        </div>
    )
}

export default AppPageTitle
